<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/new_thought_icon"
        android:title="New thought button"
        android:onClick="toThoughtEditor"
        android:icon="@drawable/thought_bubble"
        app:showAsAction="always"/>
    <item
        android:id="@+id/category_icon"
        android:title="To categories button"
        android:onClick="toCategories"
        android:icon="@drawable/category_icon"
        app:showAsAction="always" />
    <item
        android:id="@+id/entry_list_menu_settings"
        android:title="@string/settings_label"
        app:showAsAction="never" />

</menu>